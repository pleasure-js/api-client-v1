import { default as apiDriver, getDriver } from 'ui/lib/driver'
import pleasureClient, { PleasureClient } from 'ui/lib/pleasure-client'

export default {
  apiDriver,
  getDriver,
  pleasureClient,
  PleasureClient
}
